import { ProductoCiudadDto } from './producto-ciudad-dto';

describe('ProductoCiudadDto', () => {
  it('should create an instance', () => {
    expect(new ProductoCiudadDto()).toBeTruthy();
  });
});
